FIX SIDEBAR — remove multiple inner scrollbars (“slices”), replace top logo with clean typographic header like mockup

Context:
Sidebar currently shows multiple vertical scrollbars next to each menu group (looks very bad). Also the top header has a large logo card we want to remove. We want a robust rounded sidebar panel, but with ONLY ONE scrollbar (if needed) for the entire sidebar. And we want a simple brand header like the mockup: small icon + “FastDSO” text + subtitle, plus a divider line.

Strict rules:
- Keep menu order, routes, permissions logic, and collapse behavior unchanged.
- Only edit base.html (sidebar header markup) and style.css (sidebar styles).
- Do NOT change backend.

PART A — Remove “logo card” and rebuild sidebar header (like mockup)
1) In templates/base.html:
- Remove the large logo image block at the top of the sidebar.
- Replace it with a compact brand header:

<div class="sb-brand">
  <div class="sb-brand-icon">
    <i class="bi bi-grid-3x3-gap-fill"></i>
  </div>
  <div class="sb-brand-text">
    <div class="sb-brand-title">FastDSO</div>
    <div class="sb-brand-subtitle">Fast Distribution & Sales Operation</div>
  </div>
</div>
<div class="sb-divider"></div>

- Do NOT wrap this in a white rectangle or card.
- Keep it left-aligned like the mockup.

PART B — Fix scrollbars: ONLY ONE scroll for sidebar
Problem: Multiple “slices” appear because multiple elements have overflow-y: auto.
Fix:
1) Sidebar container should be a rounded panel, but only ONE scroll area.
2) Remove overflow-y from nav groups, submenus, and any inner containers.
3) Implement ONE scroll container for the nav content:
- The sidebar itself stays fixed height.
- Inside it, create one container that scrolls:

In base.html (minimal structural change allowed):
- Wrap the navigation groups in:
  <div class="sb-scroll"> ...all nav groups... </div>
- Keep header and bottom user chip outside the scroll.

So structure should be:

<aside class="sidebar">
  [sb-brand header]
  <div class="sb-scroll">
    [all nav groups + links]
  </div>
  <div class="sidebar-bottom">
    [user chip + logout]
  </div>
</aside>

PART C — CSS changes (style.css)
1) Sidebar panel:
- border-radius: 18px
- margin: 12px
- height: calc(100vh - 24px)
- overflow: hidden (no scroll on the outer panel)

2) Scroll container:
.sb-scroll {
  overflow-y: auto;
  overflow-x: hidden;
  padding-right: 6px;      /* small space for scrollbar */
  flex: 1;
}

3) Remove inner scrollbars:
- Ensure these do NOT have overflow:
.sidebar-nav,
.nav-group,
.nav-group-toggle + .sidebar-nav,
.sidebar-bottom,
any nested containers inside sidebar
Set them to overflow: visible (or remove overflow rules).

4) Scrollbar styling (optional but recommended):
.sb-scroll::-webkit-scrollbar { width: 8px; }
.sb-scroll::-webkit-scrollbar-thumb {
  background: rgba(255,255,255,0.18);
  border-radius: 999px;
}
.sb-scroll::-webkit-scrollbar-track { background: transparent; }

5) Brand header styling:
.sb-brand { display:flex; align-items:center; gap:10px; padding: 18px 16px 10px 16px; }
.sb-brand-icon { width: 38px; height: 38px; border-radius: 12px; background: rgba(255,255,255,0.12); display:grid; place-items:center; }
.sb-brand-icon i { color: rgba(255,255,255,0.95); font-size: 18px; }
.sb-brand-title { font-weight: 800; color: rgba(255,255,255,0.95); line-height: 1; }
.sb-brand-subtitle { font-size: .78rem; color: rgba(255,255,255,0.72); margin-top: 2px; line-height: 1.2; }
.sb-divider { height: 1px; margin: 10px 16px 14px 16px; background: linear-gradient(to right, rgba(255,255,255,0.35), rgba(255,255,255,0.08)); }

Acceptance criteria:
- No multiple scrollbars next to each nav group (no “slices”).
- Only one scrollbar appears for the sidebar content (if needed).
- The top logo card is gone.
- Header matches mockup style: icon + FastDSO + subtitle + divider.
- Sidebar still rounded and robust.
- Menu collapses still work.

Deliverable:
- Show the edited base.html sidebar section.
- Show the added/updated CSS in style.css.